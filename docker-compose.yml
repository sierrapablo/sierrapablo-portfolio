services:
  frontend:
    build: .
    container_name: portfolio-sierrapablo
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - reverse-proxy

networks:
  reverse-proxy:
    external: true
